export default function Docs() { return <div className="mx-auto max-w-4xl px-6 pb-24 pt-14"><div className="mono text-xs uppercase tracking-[.2em]">Method / Studionet 61999</div><h1 className="serif mt-5 text-7xl leading-none">A public record, with receipts.</h1><div className="mt-12 space-y-10 leading-8"><section><h2 className="serif text-3xl">Retroactive claims</h2><p className="mt-3">Contributors submit completed work with a repository, primary, secondary, and corroborating HTTPS source. The evidence is frozen in the epoch record.</p></section><section><h2 className="serif text-3xl">Consensus and weights</h2><p className="mt-3">The leader and validator independently fetch the configured sources, compare decision-bearing fields, and require grounded excerpts from independent registrable domains. Eligibility gates attribution, completion before cutoff, scope, duplicate signals, and minimum sources. Impact maps to bounded NONE, USEFUL, MATERIAL, CRITICAL, or FOUNDATIONAL weights.</p></section><section><h2 className="serif text-3xl">Challenges</h2><p className="mt-3">A separate wallet may submit one independent counter-evidence source during the challenge window. A challenge can reduce weight, never increase it. Unavailable or inconclusive counter-evidence preserves the original claim and records an inconclusive challenge.</p></section><section><h2 className="serif text-3xl">Settlement</h2><p className="mt-3">Pools use integer GEN wei arithmetic and epoch-local claim indexes. Native transfers are emitted as triggered child transactions on Studionet; the frontend shows the parent and child receipts. A settlement remains PENDING until the child receipt is independently observed, and the contract deliberately does not use wallet balance deltas as transfer identity.</p></section><section><h2 className="serif text-3xl">Network and contracts</h2><p className="mt-3">Backfill targets GenLayer Studionet only: chain 61999, RPC <code>https://studio.genlayer.com/api</code>, explorer <code>https://explorer-studio.genlayer.com</code>. Contract addresses are injected at deployment and displayed from canonical configuration.</p></section></div></div>; }
