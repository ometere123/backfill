import Link from "next/link"; import {ArrowUpRight,CheckCircle2} from "lucide-react";
export default async function Claim({params}:{params:Promise<{id:string}>}){const {id}=await params;return <div className="mx-auto max-w-5xl px-6 pb-24 pt-14"><div className="mono text-xs uppercase">Dossier {id} / consensus record</div><h1 className="serif mt-5 text-6xl leading-none md:text-8xl">A release pipeline fix that unblocked the next patch.</h1><div className="mt-12 grid gap-8 md:grid-cols-[1fr_.7fr]"><article><div className="plate p-7"><div className="flex items-center gap-2 mono text-xs"><CheckCircle2 size={16} className="text-[var(--moss)]"/> ELIGIBLE / MATERIAL</div><p className="serif mt-12 text-3xl">Consensus grounded the attribution, pre-cutoff completion and scope match in independent public sources.</p><div className="mt-10 border-t border-[var(--line)] pt-5"><div className="mono text-xs uppercase">Grounded excerpt / source 1</div><p className="mt-3 leading-7">“The change was merged and released as part of the maintenance cycle.”</p></div></div><Link className="button mt-6" href={`/challenge/${id}`}>Challenge the record <ArrowUpRight size={18}/></Link></article><aside className="border-l border-[var(--ink)] p-6"><div className="mono text-xs uppercase">Impact band</div><div className="serif mt-4 text-5xl">3×</div><p className="mt-3 leading-7">MATERIAL is a bounded weight, not a score. The pool performs the math after finalization.</p></aside></div></div>}
