"use client";
import {useState} from "react"; import {useParams} from "next/navigation"; import {z} from "zod"; import {ArrowUpRight} from "lucide-react"; import {TxLifecycle} from "@/components/tx-lifecycle"; import type {TxStage} from "@/lib/genlayer/client"; import {config} from "@/lib/config"; import {connectWallet} from "@/lib/genlayer/wallet"; import {writeClient,writeAndConfirm} from "@/lib/genlayer/client";
const schema=z.object({title:z.string().min(3).max(180),type:z.string().min(2),repo:z.string().url().startsWith("https://"),primary:z.string().url().startsWith("https://"),secondary:z.string().url().startsWith("https://")});
export default function Submit(){const {epochId}=useParams<{epochId:string}>(); const [stage,setStage]=useState<TxStage>(); const [error,setError]=useState(""); const [done,setDone]=useState(false); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const parsed=schema.safeParse(Object.fromEntries(new FormData(e.currentTarget))); if(!parsed.success){setError("Add valid HTTPS URLs and a clear contribution title.");return} if(!config.rounds){setError("Rounds contract address is not configured in this deployment.");return} try{setError(""); const address=await connectWallet(); await writeAndConfirm(writeClient(address,window.ethereum!),config.rounds,"submit_claim",[Number(epochId),parsed.data.title,parsed.data.type,parsed.data.repo,parsed.data.primary,parsed.data.secondary,""],0n,setStage); setDone(true)}catch(err){setError(err instanceof Error?err.message:"Transaction failed")}} return <div className="mx-auto max-w-4xl px-6 pb-24 pt-14"><div className="mono text-xs uppercase tracking-[.2em]">Epoch {epochId} / contribution intake</div><h1 className="serif mt-5 text-6xl leading-none md:text-8xl">Show us what already happened.</h1><p className="mt-7 max-w-2xl text-lg leading-8">Your evidence is public, frozen at submission, and independently checked. The model never chooses a payout amount.</p><form onSubmit={submit} className="intake-form mt-14 grid gap-5"><label>Contribution title<input name="title" placeholder="A release pipeline fix that unblocked…"/></label><label>Contribution type<input name="type" placeholder="release engineering"/></label><label>Canonical repository URL<input name="repo" placeholder="https://github.com/org/repo"/></label><label>Primary contribution URL<input name="primary" placeholder="https://github.com/org/repo/pull/123"/></label><label>Secondary evidence URL<input name="secondary" placeholder="https://github.com/org/repo/releases/tag/v1.2.3"/></label>{error&&<p className="border-l-4 border-[var(--coral)] bg-[#ff5a4715] p-4 text-sm">{error}</p>}<button className="button primary w-fit" type="submit">Freeze evidence <ArrowUpRight size={18}/></button></form><TxLifecycle stage={stage}/>{done&&<p className="mt-5">Claim submitted after execution confirmation. <a className="underline" href="/claim/1">Open dossier</a></p>}</div>}
