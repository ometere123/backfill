import Link from "next/link"; export default async function Challenge({params}:{params:Promise<{id:string}>}){const {id}=await params;return <div className="mx-auto max-w-3xl px-6 pb-24 pt-14"><div className="mono text-xs uppercase">Dossier {id} / bounded challenge</div><h1 className="serif mt-5 text-7xl leading-none">Add counter-evidence.</h1><p className="mt-7 text-lg leading-8">One public URL. One reason. The original frozen evidence remains visible; a successful challenge can reduce or remove weight, never increase your payout.</p><form className="challenge-form mt-12 grid gap-5"><label>Reason<select><option>wrong attribution</option><option>work after cutoff</option><option>duplicated contribution</option><option>out of scope</option><option>overstated impact</option></select></label><label>Counter-evidence URL<input placeholder="https://…"/></label><button className="button coral w-fit">Freeze challenge</button></form><Link className="mt-8 inline-block underline" href={`/claim/${id}`}>Return to dossier</Link></div>}
