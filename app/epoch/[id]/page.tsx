import Link from "next/link"; import {ArrowUpRight,ExternalLink} from "lucide-react"; import {ContractAction} from "@/components/contract-action";
export default async function Epoch({params}:{params:Promise<{id:string}>}){const {id}=await params; return <div className="mx-auto max-w-7xl px-6 pb-24 pt-14"><div className="flex flex-wrap justify-between gap-4 mono text-xs uppercase tracking-[.2em]"><span>Epoch {id} / open</span><span>policy sealed / cutoff 2025—12—31</span></div><h1 className="serif mt-6 max-w-5xl text-6xl leading-none md:text-8xl">Critical maintenance, quietly shipped.</h1><p className="mt-8 max-w-2xl text-xl leading-8">A retroactive round for the infrastructure work that keeps open-source systems dependable: patches, releases, interoperability and documentation that unlock real use.</p><div className="mt-16 grid gap-10 md:grid-cols-[.8fr_1.2fr]"><aside className="border-t-2 border-[var(--ink)] pt-5"><div className="mono text-xs uppercase">Allocation field</div><div className="serif mt-5 text-6xl">0 GEN</div><p className="mt-4 leading-7">Fund this pool, then watch the finalized weights allocate it proportionally.</p><ContractAction contract="pool" method="fund" args={[Number(id)]} value={1n} label="Fund pool with 1 GEN"/></aside><section><div className="mb-4 flex justify-between border-b border-[var(--ink)] pb-3 mono text-xs uppercase"><span>Evidence dossiers</span><span>01—03</span></div><article className="plate p-7"><div className="mono text-xs">Contribution plate / awaiting claims</div><h2 className="serif mt-12 text-4xl">The ledger is open to work already in the world.</h2><p className="mt-4 max-w-xl leading-7">Submit a primary contribution URL, the repository it belongs to, and independent corroboration. The contract freezes the evidence before consensus evaluates it.</p><Link className="button primary mt-8" href={`/submit/${id}`}>Enter a completed contribution <ArrowUpRight size={18}/></Link></article><div className="mt-8 flex gap-5 mono text-xs"><span>Scope manifesto</span><a className="underline" href="https://github.com" target="_blank">Public source policy <ExternalLink size={12} className="inline"/></a></div></section></div></div>}
