import Link from "next/link";
import {ArrowUpRight, BookOpen, CircleDollarSign, ShieldCheck} from "lucide-react";
export default function Home(){return <div className="grain"><section className="mx-auto max-w-7xl px-6 pb-24 pt-16 md:pt-28"><div className="grid gap-12 md:grid-cols-[1.2fr_.8fr] md:items-end"><div><div className="mono mb-8 text-xs uppercase tracking-[.25em]">Issue 01 / retroactive public goods</div><h1 className="serif max-w-4xl text-6xl leading-[.94] md:text-8xl">Fund the work after it proves its worth.</h1><p className="mt-8 max-w-xl text-lg leading-8">No promise deck. No pre-award pitch. Completed work enters. Public evidence decides.</p><div className="mt-10 flex flex-wrap gap-3"><Link className="button primary" href="/epochs">Open the current epoch <ArrowUpRight size={18}/></Link><Link className="button" href="/epochs">Submit completed work</Link></div></div><div className="plate relative p-6 md:rotate-2"><div className="mono text-xs uppercase">Backfill ledger / 2026</div><div className="my-10 flex items-center justify-between"><span className="serif text-5xl">←</span><div className="h-px flex-1 bg-[var(--ink)]"/><span className="serif text-5xl">→</span></div><p className="serif text-3xl leading-tight">A public record for the maintenance that keeps the commons alive.</p><div className="mt-10 grid grid-cols-3 gap-2 text-center mono text-[10px] uppercase"><span className="border border-[var(--ink)] p-2">Evidence</span><span className="border border-[var(--ink)] p-2">Consensus</span><span className="border border-[var(--ink)] p-2">Settlement</span></div></div></div></section><section className="border-y border-[var(--ink)] bg-[#e9e0d1]"><div className="mx-auto grid max-w-7xl gap-0 md:grid-cols-3">{[[BookOpen,"Already happened","Claims point to public work that predates the round."],[ShieldCheck,"Evidence decides","Validators independently read the same public sources."],[CircleDollarSign,"Weights settle","Impact becomes a bounded weight. The pool settles deterministically."]].map(([Icon,title,body],i)=>{const I=Icon as typeof BookOpen;return <div key={i} className="border-r border-[var(--ink)] p-8 last:border-r-0"><I size={22}/><h2 className="serif mt-10 text-3xl">{title as string}</h2><p className="mt-3 leading-7">{body as string}</p></div>})}</div></section></div>}
