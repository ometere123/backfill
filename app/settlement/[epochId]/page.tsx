import {ContractAction} from "@/components/contract-action";
export default async function Settlement({params}:{params:Promise<{epochId:string}>}){const {epochId}=await params;return <div className="mx-auto max-w-6xl px-6 pb-24 pt-14"><div className="mono text-xs uppercase">Epoch {epochId} / settlement</div><h1 className="serif mt-5 text-7xl leading-none">The pool has a shape now.</h1><p className="mt-7 max-w-2xl text-lg leading-8">Finalized impact bands reflow the allocation ribbon. Every amount is deterministic, bounded by funded GEN, and claimable once.</p><div className="mt-16 plate overflow-hidden"><div className="flex h-28"><div className="w-[58%] bg-[var(--blue)]"/><div className="w-[27%] bg-[var(--coral)]"/><div className="w-[15%] bg-[var(--moss)]"/></div><div className="grid gap-6 p-7 md:grid-cols-3"><div><div className="mono text-xs">58%</div><p className="serif text-2xl">Critical patch</p></div><div><div className="mono text-xs">27%</div><p className="serif text-2xl">Material release</p></div><div><div className="mono text-xs">15%</div><p className="serif text-2xl">Useful docs</p></div></div></div><div className="mt-8"><ContractAction contract="pool" method="claim" args={[Number(epochId),1]} label="Claim finalized GEN"/></div></div>}
