import {createClient} from "genlayer-js"; import {studionet} from "genlayer-js/chains"; import {TransactionStatus,ExecutionResult} from "genlayer-js/types"; import {config} from "@/lib/config";
export type TxStage="AWAITING_SIGNATURE"|"SUBMITTED"|"CONSENSUS"|"ACCEPTED/FINALIZED"|"EXECUTION_CONFIRMED"|"USER_REJECTED"|"WRONG_NETWORK"|"CONTRACT_ERROR"|"CONSENSUS_FAILURE"|"EXECUTION_ERROR"|"RPC_UNAVAILABLE";
export const readClient=createClient({chain:studionet});
export function writeClient(address:string,provider:NonNullable<Window["ethereum"]>){return createClient({chain:studionet,account:address as `0x${string}`,provider});}
export async function readContract(address:string,functionName:string,args:any[]=[]){if(!address) throw new Error("Contract address is not configured in this deployment"); return readClient.readContract({address:address as `0x${string}`,functionName,args});}
export async function readEpoch(id:number){return readContract(config.rounds,"get_epoch",[id]);}
export function explorerTx(hash:string){return `${config.explorer}/tx/${hash}`;}
export async function writeAndConfirm(client:any,address:string,functionName:string,args:any[],value=0n,onStage?:(s:TxStage)=>void){try{onStage?.("AWAITING_SIGNATURE"); await client.connect("studionet"); const hash=await client.writeContract({address:address as `0x${string}`,functionName,args,value}); onStage?.("SUBMITTED"); onStage?.("CONSENSUS"); const receipt=await readClient.waitForTransactionReceipt({hash,status:TransactionStatus.FINALIZED}); onStage?.("ACCEPTED/FINALIZED"); if(receipt.txExecutionResultName!==ExecutionResult.FINISHED_WITH_RETURN){onStage?.("EXECUTION_ERROR"); throw new Error("Consensus finalized but contract execution failed");} onStage?.("EXECUTION_CONFIRMED"); return {hash,receipt};}catch(e){const message=e instanceof Error?e.message:String(e); if(message.toLowerCase().includes("reject")){onStage?.("USER_REJECTED")} else if(message.toLowerCase().includes("consensus")){onStage?.("CONSENSUS_FAILURE")} else if(message.toLowerCase().includes("network")){onStage?.("WRONG_NETWORK")} else {onStage?.("CONTRACT_ERROR")} throw e;}}
